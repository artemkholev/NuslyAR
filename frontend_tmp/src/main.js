import { renderHeader } from "../src/widgets/header/index.js";
// import { renderAboutSection } from "../widgets/AboutSection";
// import { renderFooter } from "../widgets/Footer";
// import { renderChangeBackgroundButton } from "../features/ChangeBackground";

const app = document.getElementById("app");

app.appendChild(renderHeader());
// app.appendChild(renderAboutSection());
// app.appendChild(renderFooter());
// app.appendChild(renderChangeBackgroundButton());
